name: fridgenius
description: "Fridgenius! is a Gemini-powered mobile app that transforms whatever ingredients you already have in your kitchen into ranked recipe suggestions, shareable community posts, and a daily cooking habit."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  camera: ^0.12.0+2
  cloud_firestore: ^6.7.0
  firebase_ai: ^3.14.1
  firebase_app_check: ^0.4.5+2
  firebase_auth: ^6.5.6
  firebase_core: ^4.12.1
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  freezed: ^3.2.6-dev.1
  freezed_annotation: ^3.1.0
  go_router: ^17.3.0
  google_fonts: ^8.1.0
  google_mlkit_image_labeling: ^0.15.0
  hooks_riverpod: ^3.3.2
  json_annotation: ^4.12.0
  riverpod_annotation: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  riverpod_generator: ^4.0.4
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true

  assets:
    - assets/
